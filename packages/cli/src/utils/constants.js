export const VERSION = "0.0.900-Experimental";

export const FRAMEWORKS = new Set(["react", "next"]);

export const PACKAGE_MANAGERS = new Set(["npm", "pnpm", "yarn"]);

export const DEFAULT_OPTIONS = {
  cwd: ".",
  install: false,
  packageManager: "npm",
};
