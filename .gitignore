# Node modules
node_modules/

# Build output
dist/
build/
out/

# TypeScript
*.tsbuildinfo

# Vite cache
.vite/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# macOS
.DS_Store

# IDEs
.vscode/
.idea/

# Env files
.env
.env.*.local

# Testing
coverage/
jest-test-results.json

# Lint/formatter
.eslintcache
.stylelintcache

# Optional: Docs output
docs/.vitepress/dist/
docs/.next/

# Optional: Storybook
.storybook-out/
.storybook-static/
storybook-next-json/
storybook-core-json/

# Optional: Local tooling
yalc.lock
.yalc/

# Optional: Pkg manager files
package-lock.json
pnpm-lock.yaml
yarn.lock

# Build artifacts
*.tgz

# VS Code workspace config (optional)
*.code-workspace

# Lint results (if generated)
eslint-report.json
stylelint-report.json
DESIGN.md
debug-storybook.log
cypress/screenshots/Accordion.cy.tsx/core Accordion behavior -- has no detectable accessibility violations when collapsed (failed).png
cypress/screenshots/Accordion.cy.tsx/core Accordion behavior -- has no detectable accessibility violations when disabled (failed).png
cypress/screenshots/Accordion.cy.tsx/core Accordion behavior -- has no detectable accessibility violations when expanded (failed).png
cypress/screenshots/Accordion.cy.tsx/core Accordion behavior -- is always expanded when isToggleable is false (failed).png
cypress/screenshots/Accordion.cy.tsx/core Accordion behavior -- supports keyboard toggling with Enter and Space and calls onToggle (failed).png
cypress/screenshots/Accordion.cy.tsx/core Accordion behavior -- toggles open and closed when clicked and calls onToggle with the next expanded state (failed).png
cypress/screenshots/Accordion.cy.tsx/next Accordion behavior -- has no detectable accessibility violations when collapsed (failed).png
cypress/screenshots/Accordion.cy.tsx/next Accordion behavior -- has no detectable accessibility violations when disabled (failed).png
cypress/screenshots/Accordion.cy.tsx/next Accordion behavior -- has no detectable accessibility violations when expanded (failed).png
cypress/screenshots/Accordion.cy.tsx/next Accordion behavior -- is always expanded when isToggleable is false (failed).png
cypress/screenshots/Accordion.cy.tsx/next Accordion behavior -- supports keyboard toggling with Enter and Space and calls onToggle (failed).png
cypress/screenshots/Accordion.cy.tsx/next Accordion behavior -- toggles open and closed when clicked and calls onToggle with the next expanded state (failed).png
